# Profile: Codebase Context (Use Case B)
#
# Optimised for fast retrieval of code understanding to assist coding
# agents (code review, refactoring, navigation).  Speed-first: the
# cross-encoder reranker is DISABLED (Experiment 10 showed it degrades
# Coverage@20 by 19-27% on identifier-heavy technical docs and adds
# ~14s latency per query), retrieval is hybrid (dense + BM25 + RRF for
# exact-match keyword coverage), and file_type taxonomy classifies by
# Magika-detected language.
#
# Bind a collection to this profile:
#   metadata={"profile": "codebase"}
#
# NEVER Put API keys, tokens, or secrets in this file.

# Retrieval: speed-first for coding agents in the loop.
retrieval:
  top_k: 20
  rerank_enabled: false
  hybrid_enabled: true

# Chunking fallback for ambiguous file types.
chunking:
  strategy_fallback: code

# Metadata taxonomy: file-type classification via Magika.
metadata:
  taxonomy_mode: file_type

# Ingestible extensions (fix-embedding-and-structure-fidelity-1, design
# D4): the document seven PLUS source extensions for the languages the
# AST extractor maps to tree-sitter grammars
# (core/codebase/ast_extract.py, MAGIKA_LABEL_TO_TREESITTER). Without
# this the chunking.strategy_fallback above is unreachable: source files
# are never collected, so the AST-aware path cannot run. Override with
# INGESTION__INGEST_EXTENSIONS when a narrower or wider set is needed.
ingestion:
  ingest_extensions:
    # Document formats (the global default set).
    - .pdf
    - .docx
    - .pptx
    - .txt
    - .md
    - .html
    - .csv
    # Source languages with tree-sitter mappings.
    - .py
    - .js
    - .jsx
    - .ts
    - .tsx
    - .java
    - .c
    - .h
    - .cpp
    - .cc
    - .hpp
    - .cs
    - .go
    - .rs
    - .rb
    - .php
    - .swift
    - .kt
    - .scala
    - .css
    - .sql
    - .sh
    - .bash
    - .yaml
    - .yml
    - .toml
    - .json
