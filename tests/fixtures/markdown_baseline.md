# Baseline Markdown Fixture

Intro paragraph with enough words to matter for the boundary probe run.

## Alpha Section

Alpha body text mentions OMRG-ALPHA-77 as its identifier-heavy token.

## Beta Section

- beta item one
- beta item two
- beta item three

## Gamma Table

| name | qty | price |
|------|-----|-------|
| bolt | 12  | 0.05  |
| nut  | 48  | 0.02  |

### Delta Long Section

The delta section repeats dense technical prose to exceed the small token budget used by this baseline probe and force sub-splitting. The delta section repeats dense technical prose to exceed the small token budget used by this baseline probe and force sub-splitting. The delta section repeats dense technical prose to exceed the small token budget used by this baseline probe and force sub-splitting. The delta section repeats dense technical prose to exceed the small token budget used by this baseline probe and force sub-splitting. The delta section repeats dense technical prose to exceed the small token budget used by this baseline probe and force sub-splitting. The delta section repeats dense technical prose to exceed the small token budget used by this baseline probe and force sub-splitting. The delta section repeats dense technical prose to exceed the small token budget used by this baseline probe and force sub-splitting. The delta section repeats dense technical prose to exceed the small token budget used by this baseline probe and force sub-splitting. The delta section repeats dense technical prose to exceed the small token budget used by this baseline probe and force sub-splitting. The delta section repeats dense technical prose to exceed the small token budget used by this baseline probe and force sub-splitting.
