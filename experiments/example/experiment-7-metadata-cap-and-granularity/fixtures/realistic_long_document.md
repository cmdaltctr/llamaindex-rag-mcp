# Long Document Evaluation Corpus

A realistic Markdown article for metadata-extraction budget tests. Reference marker MARK1000X opens the corpus.

## Retrieval notes 00

Retrieval quality depends on the corpus, the embedder, and the ranking policy.

The pipeline reads the document once and splits it with the declared splitter. Every chunk inherits the file-level metadata dictionary after aggregation. The extractor budget bounds how many chunks reach the expensive stage. Deterministic fixtures keep the comparison reproducible across reruns. The cap is expressed in chunk units rather than character units.
Reference marker MARK1001X.

Every chunk inherits the file-level metadata dictionary after aggregation. The extractor budget bounds how many chunks reach the expensive stage. Deterministic fixtures keep the comparison reproducible across reruns. The cap is expressed in chunk units rather than character units. Section markers let the harness attribute each call to its source chunk.
Reference marker MARK1002X.

The extractor budget bounds how many chunks reach the expensive stage. Deterministic fixtures keep the comparison reproducible across reruns. The cap is expressed in chunk units rather than character units. Section markers let the harness attribute each call to its source chunk. The pipeline reads the document once and splits it with the declared splitter.
Reference marker MARK1003X.

- First bullet restates the section topic.
- Second bullet notes the paired comparison.
- Third bullet records the marker attribution.

## Chunking notes 01

Chunk boundaries decide whether a retrieved passage answers the question alone.

Every chunk inherits the file-level metadata dictionary after aggregation. The extractor budget bounds how many chunks reach the expensive stage. Deterministic fixtures keep the comparison reproducible across reruns. The cap is expressed in chunk units rather than character units. Section markers let the harness attribute each call to its source chunk.
Reference marker MARK1004X.

The extractor budget bounds how many chunks reach the expensive stage. Deterministic fixtures keep the comparison reproducible across reruns. The cap is expressed in chunk units rather than character units. Section markers let the harness attribute each call to its source chunk. The pipeline reads the document once and splits it with the declared splitter.
Reference marker MARK1005X.

Deterministic fixtures keep the comparison reproducible across reruns. The cap is expressed in chunk units rather than character units. Section markers let the harness attribute each call to its source chunk. The pipeline reads the document once and splits it with the declared splitter. Every chunk inherits the file-level metadata dictionary after aggregation.
Reference marker MARK1006X.

- First bullet restates the section topic.
- Second bullet notes the paired comparison.
- Third bullet records the marker attribution.

## Metadata notes 02

File-level metadata gives every stored chunk the same provenance labels.

The extractor budget bounds how many chunks reach the expensive stage. Deterministic fixtures keep the comparison reproducible across reruns. The cap is expressed in chunk units rather than character units. Section markers let the harness attribute each call to its source chunk. The pipeline reads the document once and splits it with the declared splitter.
Reference marker MARK1007X.

Deterministic fixtures keep the comparison reproducible across reruns. The cap is expressed in chunk units rather than character units. Section markers let the harness attribute each call to its source chunk. The pipeline reads the document once and splits it with the declared splitter. Every chunk inherits the file-level metadata dictionary after aggregation.
Reference marker MARK1008X.

The cap is expressed in chunk units rather than character units. Section markers let the harness attribute each call to its source chunk. The pipeline reads the document once and splits it with the declared splitter. Every chunk inherits the file-level metadata dictionary after aggregation. The extractor budget bounds how many chunks reach the expensive stage.
Reference marker MARK1009X.

- First bullet restates the section topic.
- Second bullet notes the paired comparison.
- Third bullet records the marker attribution.

## Evaluation notes 03

Paired designs remove corpus variance when comparing two retrieval arms.

Deterministic fixtures keep the comparison reproducible across reruns. The cap is expressed in chunk units rather than character units. Section markers let the harness attribute each call to its source chunk. The pipeline reads the document once and splits it with the declared splitter. Every chunk inherits the file-level metadata dictionary after aggregation.
Reference marker MARK1010X.

The cap is expressed in chunk units rather than character units. Section markers let the harness attribute each call to its source chunk. The pipeline reads the document once and splits it with the declared splitter. Every chunk inherits the file-level metadata dictionary after aggregation. The extractor budget bounds how many chunks reach the expensive stage.
Reference marker MARK1011X.

Section markers let the harness attribute each call to its source chunk. The pipeline reads the document once and splits it with the declared splitter. Every chunk inherits the file-level metadata dictionary after aggregation. The extractor budget bounds how many chunks reach the expensive stage. Deterministic fixtures keep the comparison reproducible across reruns.
Reference marker MARK1012X.

- First bullet restates the section topic.
- Second bullet notes the paired comparison.
- Third bullet records the marker attribution.

## Thresholds notes 04

A similarity threshold trades coverage for precision on every query.

The cap is expressed in chunk units rather than character units. Section markers let the harness attribute each call to its source chunk. The pipeline reads the document once and splits it with the declared splitter. Every chunk inherits the file-level metadata dictionary after aggregation. The extractor budget bounds how many chunks reach the expensive stage.
Reference marker MARK1013X.

Section markers let the harness attribute each call to its source chunk. The pipeline reads the document once and splits it with the declared splitter. Every chunk inherits the file-level metadata dictionary after aggregation. The extractor budget bounds how many chunks reach the expensive stage. Deterministic fixtures keep the comparison reproducible across reruns.
Reference marker MARK1014X.

The pipeline reads the document once and splits it with the declared splitter. Every chunk inherits the file-level metadata dictionary after aggregation. The extractor budget bounds how many chunks reach the expensive stage. Deterministic fixtures keep the comparison reproducible across reruns. The cap is expressed in chunk units rather than character units.
Reference marker MARK1015X.

- First bullet restates the section topic.
- Second bullet notes the paired comparison.
- Third bullet records the marker attribution.

## Storage notes 05

Vector stores differ in distance metrics, filters, and generation semantics.

Section markers let the harness attribute each call to its source chunk. The pipeline reads the document once and splits it with the declared splitter. Every chunk inherits the file-level metadata dictionary after aggregation. The extractor budget bounds how many chunks reach the expensive stage. Deterministic fixtures keep the comparison reproducible across reruns.
Reference marker MARK1016X.

The pipeline reads the document once and splits it with the declared splitter. Every chunk inherits the file-level metadata dictionary after aggregation. The extractor budget bounds how many chunks reach the expensive stage. Deterministic fixtures keep the comparison reproducible across reruns. The cap is expressed in chunk units rather than character units.
Reference marker MARK1017X.

Every chunk inherits the file-level metadata dictionary after aggregation. The extractor budget bounds how many chunks reach the expensive stage. Deterministic fixtures keep the comparison reproducible across reruns. The cap is expressed in chunk units rather than character units. Section markers let the harness attribute each call to its source chunk.
Reference marker MARK1018X.

- First bullet restates the section topic.
- Second bullet notes the paired comparison.
- Third bullet records the marker attribution.

## Reranking notes 06

A cross encoder reorders candidates but inherits the candidate pool's blind spots.

The pipeline reads the document once and splits it with the declared splitter. Every chunk inherits the file-level metadata dictionary after aggregation. The extractor budget bounds how many chunks reach the expensive stage. Deterministic fixtures keep the comparison reproducible across reruns. The cap is expressed in chunk units rather than character units.
Reference marker MARK1019X.

Every chunk inherits the file-level metadata dictionary after aggregation. The extractor budget bounds how many chunks reach the expensive stage. Deterministic fixtures keep the comparison reproducible across reruns. The cap is expressed in chunk units rather than character units. Section markers let the harness attribute each call to its source chunk.
Reference marker MARK1020X.

The extractor budget bounds how many chunks reach the expensive stage. Deterministic fixtures keep the comparison reproducible across reruns. The cap is expressed in chunk units rather than character units. Section markers let the harness attribute each call to its source chunk. The pipeline reads the document once and splits it with the declared splitter.
Reference marker MARK1021X.

- First bullet restates the section topic.
- Second bullet notes the paired comparison.
- Third bullet records the marker attribution.

## Observability notes 07

Runtime manifests make a reported number attributable to what actually ran.

Every chunk inherits the file-level metadata dictionary after aggregation. The extractor budget bounds how many chunks reach the expensive stage. Deterministic fixtures keep the comparison reproducible across reruns. The cap is expressed in chunk units rather than character units. Section markers let the harness attribute each call to its source chunk.
Reference marker MARK1022X.

The extractor budget bounds how many chunks reach the expensive stage. Deterministic fixtures keep the comparison reproducible across reruns. The cap is expressed in chunk units rather than character units. Section markers let the harness attribute each call to its source chunk. The pipeline reads the document once and splits it with the declared splitter.
Reference marker MARK1023X.

Deterministic fixtures keep the comparison reproducible across reruns. The cap is expressed in chunk units rather than character units. Section markers let the harness attribute each call to its source chunk. The pipeline reads the document once and splits it with the declared splitter. Every chunk inherits the file-level metadata dictionary after aggregation.
Reference marker MARK1024X.

### Worked example

```python
def budget(chunks, cap):
    return chunks[:cap]
```

- First bullet restates the section topic.
- Second bullet notes the paired comparison.
- Third bullet records the marker attribution.

## Retrieval notes 08

Retrieval quality depends on the corpus, the embedder, and the ranking policy.

The extractor budget bounds how many chunks reach the expensive stage. Deterministic fixtures keep the comparison reproducible across reruns. The cap is expressed in chunk units rather than character units. Section markers let the harness attribute each call to its source chunk. The pipeline reads the document once and splits it with the declared splitter.
Reference marker MARK1025X.

Deterministic fixtures keep the comparison reproducible across reruns. The cap is expressed in chunk units rather than character units. Section markers let the harness attribute each call to its source chunk. The pipeline reads the document once and splits it with the declared splitter. Every chunk inherits the file-level metadata dictionary after aggregation.
Reference marker MARK1026X.

The cap is expressed in chunk units rather than character units. Section markers let the harness attribute each call to its source chunk. The pipeline reads the document once and splits it with the declared splitter. Every chunk inherits the file-level metadata dictionary after aggregation. The extractor budget bounds how many chunks reach the expensive stage.
Reference marker MARK1027X.

- First bullet restates the section topic.
- Second bullet notes the paired comparison.
- Third bullet records the marker attribution.

## Chunking notes 09

Chunk boundaries decide whether a retrieved passage answers the question alone.

Deterministic fixtures keep the comparison reproducible across reruns. The cap is expressed in chunk units rather than character units. Section markers let the harness attribute each call to its source chunk. The pipeline reads the document once and splits it with the declared splitter. Every chunk inherits the file-level metadata dictionary after aggregation.
Reference marker MARK1028X.

The cap is expressed in chunk units rather than character units. Section markers let the harness attribute each call to its source chunk. The pipeline reads the document once and splits it with the declared splitter. Every chunk inherits the file-level metadata dictionary after aggregation. The extractor budget bounds how many chunks reach the expensive stage.
Reference marker MARK1029X.

Section markers let the harness attribute each call to its source chunk. The pipeline reads the document once and splits it with the declared splitter. Every chunk inherits the file-level metadata dictionary after aggregation. The extractor budget bounds how many chunks reach the expensive stage. Deterministic fixtures keep the comparison reproducible across reruns.
Reference marker MARK1030X.

- First bullet restates the section topic.
- Second bullet notes the paired comparison.
- Third bullet records the marker attribution.

## Metadata notes 10

File-level metadata gives every stored chunk the same provenance labels.

The cap is expressed in chunk units rather than character units. Section markers let the harness attribute each call to its source chunk. The pipeline reads the document once and splits it with the declared splitter. Every chunk inherits the file-level metadata dictionary after aggregation. The extractor budget bounds how many chunks reach the expensive stage.
Reference marker MARK1031X.

Section markers let the harness attribute each call to its source chunk. The pipeline reads the document once and splits it with the declared splitter. Every chunk inherits the file-level metadata dictionary after aggregation. The extractor budget bounds how many chunks reach the expensive stage. Deterministic fixtures keep the comparison reproducible across reruns.
Reference marker MARK1032X.

The pipeline reads the document once and splits it with the declared splitter. Every chunk inherits the file-level metadata dictionary after aggregation. The extractor budget bounds how many chunks reach the expensive stage. Deterministic fixtures keep the comparison reproducible across reruns. The cap is expressed in chunk units rather than character units.
Reference marker MARK1033X.

- First bullet restates the section topic.
- Second bullet notes the paired comparison.
- Third bullet records the marker attribution.

## Evaluation notes 11

Paired designs remove corpus variance when comparing two retrieval arms.

Section markers let the harness attribute each call to its source chunk. The pipeline reads the document once and splits it with the declared splitter. Every chunk inherits the file-level metadata dictionary after aggregation. The extractor budget bounds how many chunks reach the expensive stage. Deterministic fixtures keep the comparison reproducible across reruns.
Reference marker MARK1034X.

The pipeline reads the document once and splits it with the declared splitter. Every chunk inherits the file-level metadata dictionary after aggregation. The extractor budget bounds how many chunks reach the expensive stage. Deterministic fixtures keep the comparison reproducible across reruns. The cap is expressed in chunk units rather than character units.
Reference marker MARK1035X.

Every chunk inherits the file-level metadata dictionary after aggregation. The extractor budget bounds how many chunks reach the expensive stage. Deterministic fixtures keep the comparison reproducible across reruns. The cap is expressed in chunk units rather than character units. Section markers let the harness attribute each call to its source chunk.
Reference marker MARK1036X.

- First bullet restates the section topic.
- Second bullet notes the paired comparison.
- Third bullet records the marker attribution.

## Thresholds notes 12

A similarity threshold trades coverage for precision on every query.

The pipeline reads the document once and splits it with the declared splitter. Every chunk inherits the file-level metadata dictionary after aggregation. The extractor budget bounds how many chunks reach the expensive stage. Deterministic fixtures keep the comparison reproducible across reruns. The cap is expressed in chunk units rather than character units.
Reference marker MARK1037X.

Every chunk inherits the file-level metadata dictionary after aggregation. The extractor budget bounds how many chunks reach the expensive stage. Deterministic fixtures keep the comparison reproducible across reruns. The cap is expressed in chunk units rather than character units. Section markers let the harness attribute each call to its source chunk.
Reference marker MARK1038X.

The extractor budget bounds how many chunks reach the expensive stage. Deterministic fixtures keep the comparison reproducible across reruns. The cap is expressed in chunk units rather than character units. Section markers let the harness attribute each call to its source chunk. The pipeline reads the document once and splits it with the declared splitter.
Reference marker MARK1039X.

- First bullet restates the section topic.
- Second bullet notes the paired comparison.
- Third bullet records the marker attribution.

## Storage notes 13

Vector stores differ in distance metrics, filters, and generation semantics.

Every chunk inherits the file-level metadata dictionary after aggregation. The extractor budget bounds how many chunks reach the expensive stage. Deterministic fixtures keep the comparison reproducible across reruns. The cap is expressed in chunk units rather than character units. Section markers let the harness attribute each call to its source chunk.
Reference marker MARK1040X.

The extractor budget bounds how many chunks reach the expensive stage. Deterministic fixtures keep the comparison reproducible across reruns. The cap is expressed in chunk units rather than character units. Section markers let the harness attribute each call to its source chunk. The pipeline reads the document once and splits it with the declared splitter.
Reference marker MARK1041X.

Deterministic fixtures keep the comparison reproducible across reruns. The cap is expressed in chunk units rather than character units. Section markers let the harness attribute each call to its source chunk. The pipeline reads the document once and splits it with the declared splitter. Every chunk inherits the file-level metadata dictionary after aggregation.
Reference marker MARK1042X.

| stage | unit | bounded by |
|---|---|---|
| split | chunk | splitter settings |
| extract | chunk | configured cap |
| store | file | aggregated metadata |

- First bullet restates the section topic.
- Second bullet notes the paired comparison.
- Third bullet records the marker attribution.

## Reranking notes 14

A cross encoder reorders candidates but inherits the candidate pool's blind spots.

The extractor budget bounds how many chunks reach the expensive stage. Deterministic fixtures keep the comparison reproducible across reruns. The cap is expressed in chunk units rather than character units. Section markers let the harness attribute each call to its source chunk. The pipeline reads the document once and splits it with the declared splitter.
Reference marker MARK1043X.

Deterministic fixtures keep the comparison reproducible across reruns. The cap is expressed in chunk units rather than character units. Section markers let the harness attribute each call to its source chunk. The pipeline reads the document once and splits it with the declared splitter. Every chunk inherits the file-level metadata dictionary after aggregation.
Reference marker MARK1044X.

The cap is expressed in chunk units rather than character units. Section markers let the harness attribute each call to its source chunk. The pipeline reads the document once and splits it with the declared splitter. Every chunk inherits the file-level metadata dictionary after aggregation. The extractor budget bounds how many chunks reach the expensive stage.
Reference marker MARK1045X.

- First bullet restates the section topic.
- Second bullet notes the paired comparison.
- Third bullet records the marker attribution.

## Observability notes 15

Runtime manifests make a reported number attributable to what actually ran.

Deterministic fixtures keep the comparison reproducible across reruns. The cap is expressed in chunk units rather than character units. Section markers let the harness attribute each call to its source chunk. The pipeline reads the document once and splits it with the declared splitter. Every chunk inherits the file-level metadata dictionary after aggregation.
Reference marker MARK1046X.

The cap is expressed in chunk units rather than character units. Section markers let the harness attribute each call to its source chunk. The pipeline reads the document once and splits it with the declared splitter. Every chunk inherits the file-level metadata dictionary after aggregation. The extractor budget bounds how many chunks reach the expensive stage.
Reference marker MARK1047X.

Section markers let the harness attribute each call to its source chunk. The pipeline reads the document once and splits it with the declared splitter. Every chunk inherits the file-level metadata dictionary after aggregation. The extractor budget bounds how many chunks reach the expensive stage. Deterministic fixtures keep the comparison reproducible across reruns.
Reference marker MARK1048X.

- First bullet restates the section topic.
- Second bullet notes the paired comparison.
- Third bullet records the marker attribution.

## Retrieval notes 16

Retrieval quality depends on the corpus, the embedder, and the ranking policy.

The cap is expressed in chunk units rather than character units. Section markers let the harness attribute each call to its source chunk. The pipeline reads the document once and splits it with the declared splitter. Every chunk inherits the file-level metadata dictionary after aggregation. The extractor budget bounds how many chunks reach the expensive stage.
Reference marker MARK1049X.

Section markers let the harness attribute each call to its source chunk. The pipeline reads the document once and splits it with the declared splitter. Every chunk inherits the file-level metadata dictionary after aggregation. The extractor budget bounds how many chunks reach the expensive stage. Deterministic fixtures keep the comparison reproducible across reruns.
Reference marker MARK1050X.

The pipeline reads the document once and splits it with the declared splitter. Every chunk inherits the file-level metadata dictionary after aggregation. The extractor budget bounds how many chunks reach the expensive stage. Deterministic fixtures keep the comparison reproducible across reruns. The cap is expressed in chunk units rather than character units.
Reference marker MARK1051X.

- First bullet restates the section topic.
- Second bullet notes the paired comparison.
- Third bullet records the marker attribution.

## Chunking notes 17

Chunk boundaries decide whether a retrieved passage answers the question alone.

Section markers let the harness attribute each call to its source chunk. The pipeline reads the document once and splits it with the declared splitter. Every chunk inherits the file-level metadata dictionary after aggregation. The extractor budget bounds how many chunks reach the expensive stage. Deterministic fixtures keep the comparison reproducible across reruns.
Reference marker MARK1052X.

The pipeline reads the document once and splits it with the declared splitter. Every chunk inherits the file-level metadata dictionary after aggregation. The extractor budget bounds how many chunks reach the expensive stage. Deterministic fixtures keep the comparison reproducible across reruns. The cap is expressed in chunk units rather than character units.
Reference marker MARK1053X.

Every chunk inherits the file-level metadata dictionary after aggregation. The extractor budget bounds how many chunks reach the expensive stage. Deterministic fixtures keep the comparison reproducible across reruns. The cap is expressed in chunk units rather than character units. Section markers let the harness attribute each call to its source chunk.
Reference marker MARK1054X.

- First bullet restates the section topic.
- Second bullet notes the paired comparison.
- Third bullet records the marker attribution.

## Metadata notes 18

File-level metadata gives every stored chunk the same provenance labels.

The pipeline reads the document once and splits it with the declared splitter. Every chunk inherits the file-level metadata dictionary after aggregation. The extractor budget bounds how many chunks reach the expensive stage. Deterministic fixtures keep the comparison reproducible across reruns. The cap is expressed in chunk units rather than character units.
Reference marker MARK1055X.

Every chunk inherits the file-level metadata dictionary after aggregation. The extractor budget bounds how many chunks reach the expensive stage. Deterministic fixtures keep the comparison reproducible across reruns. The cap is expressed in chunk units rather than character units. Section markers let the harness attribute each call to its source chunk.
Reference marker MARK1056X.

The extractor budget bounds how many chunks reach the expensive stage. Deterministic fixtures keep the comparison reproducible across reruns. The cap is expressed in chunk units rather than character units. Section markers let the harness attribute each call to its source chunk. The pipeline reads the document once and splits it with the declared splitter.
Reference marker MARK1057X.

- First bullet restates the section topic.
- Second bullet notes the paired comparison.
- Third bullet records the marker attribution.

## Evaluation notes 19

Paired designs remove corpus variance when comparing two retrieval arms.

Every chunk inherits the file-level metadata dictionary after aggregation. The extractor budget bounds how many chunks reach the expensive stage. Deterministic fixtures keep the comparison reproducible across reruns. The cap is expressed in chunk units rather than character units. Section markers let the harness attribute each call to its source chunk.
Reference marker MARK1058X.

The extractor budget bounds how many chunks reach the expensive stage. Deterministic fixtures keep the comparison reproducible across reruns. The cap is expressed in chunk units rather than character units. Section markers let the harness attribute each call to its source chunk. The pipeline reads the document once and splits it with the declared splitter.
Reference marker MARK1059X.

Deterministic fixtures keep the comparison reproducible across reruns. The cap is expressed in chunk units rather than character units. Section markers let the harness attribute each call to its source chunk. The pipeline reads the document once and splits it with the declared splitter. Every chunk inherits the file-level metadata dictionary after aggregation.
Reference marker MARK1060X.

- First bullet restates the section topic.
- Second bullet notes the paired comparison.
- Third bullet records the marker attribution.

## Thresholds notes 20

A similarity threshold trades coverage for precision on every query.

The extractor budget bounds how many chunks reach the expensive stage. Deterministic fixtures keep the comparison reproducible across reruns. The cap is expressed in chunk units rather than character units. Section markers let the harness attribute each call to its source chunk. The pipeline reads the document once and splits it with the declared splitter.
Reference marker MARK1061X.

Deterministic fixtures keep the comparison reproducible across reruns. The cap is expressed in chunk units rather than character units. Section markers let the harness attribute each call to its source chunk. The pipeline reads the document once and splits it with the declared splitter. Every chunk inherits the file-level metadata dictionary after aggregation.
Reference marker MARK1062X.

The cap is expressed in chunk units rather than character units. Section markers let the harness attribute each call to its source chunk. The pipeline reads the document once and splits it with the declared splitter. Every chunk inherits the file-level metadata dictionary after aggregation. The extractor budget bounds how many chunks reach the expensive stage.
Reference marker MARK1063X.

- First bullet restates the section topic.
- Second bullet notes the paired comparison.
- Third bullet records the marker attribution.

## Storage notes 21

Vector stores differ in distance metrics, filters, and generation semantics.

Deterministic fixtures keep the comparison reproducible across reruns. The cap is expressed in chunk units rather than character units. Section markers let the harness attribute each call to its source chunk. The pipeline reads the document once and splits it with the declared splitter. Every chunk inherits the file-level metadata dictionary after aggregation.
Reference marker MARK1064X.

The cap is expressed in chunk units rather than character units. Section markers let the harness attribute each call to its source chunk. The pipeline reads the document once and splits it with the declared splitter. Every chunk inherits the file-level metadata dictionary after aggregation. The extractor budget bounds how many chunks reach the expensive stage.
Reference marker MARK1065X.

Section markers let the harness attribute each call to its source chunk. The pipeline reads the document once and splits it with the declared splitter. Every chunk inherits the file-level metadata dictionary after aggregation. The extractor budget bounds how many chunks reach the expensive stage. Deterministic fixtures keep the comparison reproducible across reruns.
Reference marker MARK1066X.

- First bullet restates the section topic.
- Second bullet notes the paired comparison.
- Third bullet records the marker attribution.

## Reranking notes 22

A cross encoder reorders candidates but inherits the candidate pool's blind spots.

The cap is expressed in chunk units rather than character units. Section markers let the harness attribute each call to its source chunk. The pipeline reads the document once and splits it with the declared splitter. Every chunk inherits the file-level metadata dictionary after aggregation. The extractor budget bounds how many chunks reach the expensive stage.
Reference marker MARK1067X.

Section markers let the harness attribute each call to its source chunk. The pipeline reads the document once and splits it with the declared splitter. Every chunk inherits the file-level metadata dictionary after aggregation. The extractor budget bounds how many chunks reach the expensive stage. Deterministic fixtures keep the comparison reproducible across reruns.
Reference marker MARK1068X.

The pipeline reads the document once and splits it with the declared splitter. Every chunk inherits the file-level metadata dictionary after aggregation. The extractor budget bounds how many chunks reach the expensive stage. Deterministic fixtures keep the comparison reproducible across reruns. The cap is expressed in chunk units rather than character units.
Reference marker MARK1069X.

- First bullet restates the section topic.
- Second bullet notes the paired comparison.
- Third bullet records the marker attribution.

## Observability notes 23

Runtime manifests make a reported number attributable to what actually ran.

Section markers let the harness attribute each call to its source chunk. The pipeline reads the document once and splits it with the declared splitter. Every chunk inherits the file-level metadata dictionary after aggregation. The extractor budget bounds how many chunks reach the expensive stage. Deterministic fixtures keep the comparison reproducible across reruns.
Reference marker MARK1070X.

The pipeline reads the document once and splits it with the declared splitter. Every chunk inherits the file-level metadata dictionary after aggregation. The extractor budget bounds how many chunks reach the expensive stage. Deterministic fixtures keep the comparison reproducible across reruns. The cap is expressed in chunk units rather than character units.
Reference marker MARK1071X.

Every chunk inherits the file-level metadata dictionary after aggregation. The extractor budget bounds how many chunks reach the expensive stage. Deterministic fixtures keep the comparison reproducible across reruns. The cap is expressed in chunk units rather than character units. Section markers let the harness attribute each call to its source chunk.
Reference marker MARK1072X.

- First bullet restates the section topic.
- Second bullet notes the paired comparison.
- Third bullet records the marker attribution.
