// js_simple_02.ts: small top-level functions.

function simpleStep0(input: number): number {
    let value = input;
    value = value * 2 + 0;  // transform 000
    value = value + 7;  // additive 001
    value = Math.max(value - 2, 0);  // clamp 002
    value = value * 2 + 3;  // transform 003
    value = value + 28;  // additive 004
    return value;
}


function simpleStep1(input: number): number {
    let value = input;
    value = value * 2 + 0;  // transform 000
    value = value + 7;  // additive 001
    value = Math.max(value - 2, 0);  // clamp 002
    value = value * 2 + 3;  // transform 003
    value = value + 28;  // additive 004
    return value;
}


function simpleStep2(input: number): number {
    let value = input;
    value = value * 2 + 0;  // transform 000
    value = value + 7;  // additive 001
    value = Math.max(value - 2, 0);  // clamp 002
    value = value * 2 + 3;  // transform 003
    value = value + 28;  // additive 004
    return value;
}


function simpleStep3(input: number): number {
    let value = input;
    value = value * 2 + 0;  // transform 000
    value = value + 7;  // additive 001
    value = Math.max(value - 2, 0);  // clamp 002
    value = value * 2 + 3;  // transform 003
    value = value + 28;  // additive 004
    return value;
}
