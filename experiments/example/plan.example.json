{
  "schema_version": 1,
  "status": "template",
  "note": "Copy beside a promoted protocol and replace placeholder values before writing the runner. The runner's generated cells and runtime manifest assertions must match this plan.",
  "required_fields": {
    "experiment_id": "string",
    "protocol_version": "string",
    "experimental_unit": "string",
    "primary_metric": "string",
    "manipulated_factors": [{"name": "factor_name", "levels": ["level_a", "level_b"]}],
    "controlled_variables": {"control_name": "frozen value or identity"},
    "blocks": [{"name": "block_name", "membership_file_or_rule": "pre-registered rule"}],
    "cells": [{"id": "cell_id", "factors": {"factor_name": "level_a"}}],
    "preflight_assertions": [{"manifest_field": "effective.path", "operator": "eq", "expected": "value"}],
    "abort_criteria": ["condition that makes a cell INVALID rather than FAIL"],
    "primary_contrasts": [{"name": "contrast_name", "treatment": "cell_a", "control": "cell_b", "practical_gate": "numeric pre-registered rule"}],
    "raw_artefacts": ["runtime_manifest.json", "raw_results.json"]
  }
}
