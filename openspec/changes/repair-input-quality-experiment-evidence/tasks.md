# Tasks: repair-input-quality-experiment-evidence

Work in the existing worktree. Continuation (2026-09-10): this change now executes in worktree `llamaindex-rag-mcp-v3-feat-repair-input-quality-experiment-evidence` on branch `feat/repair-input-quality-experiment-evidence` at HEAD `23389f7`; the recovered repair work is committed there. Read `design.md` and its decision register before continuing a session.

Tick each task immediately after its verification succeeds. Add the evidence pointer beside the task. Leave blocked work unchecked and name the blocker. No paid run, private-document scan, production routing change or default promotion follows automatically from approval to repair tooling.

Execution order: preserve state first, then reopen decisions and repair tooling. Documentation can proceed while runtime tooling is blocked. The approved remote backup in section 1 does not authorise a pull request. Section 6 requires the listed operator approvals. Section 7 records a decision and a possible amendment; it does not authorise an unspecified production patch.

Status (2026-09-10, session update):

- Experiment 28 routing fix adopted (commit `9bf4810`): `mixed` PDFs now route by the calibrated thresholds, not unconditionally. The packaged OCR default stays off. This closes the concrete routing defect experiment 28 exposed.
- Section 3 (paid experiment 25 rebuild) is descoped. The operator confirmed at task 3.7 that experiment 25 is not rerun, so the estimate, approval and budget-stop machinery is not needed. `build_index.py` keeps refusing rebuilds. See `evidence/exp25-debug-pause-2026-09-10.md` for the abandoned rebuild-debug detail.
- Section 6 (repeat the PDF routing study on an approved corpus) is deferred to a separate future change. It is new paid measurement, not evidence repair.
- The `experiment-validity-gates` delta records gate principles. Its paid-builder and repeat-PDF-study requirements belong to the descoped or deferred sections; they are not implemented completion claims.

## 1. Preserve the starting state

- [x] 1.1 Verify the worktree and branch, record HEAD and remote-tracking ref, and preserve the four-file patch. Verify the saved patch digest matches the original diff. Evidence: `evidence/apply-start.md`; saved patch equals the original 9,356-byte diff exactly (SHA-256 recorded).
- [x] 1.2 Obtain a command tool with an explicit working-directory parameter before tests or other writing commands. Verify its directory and branch checks; leave runtime work blocked if unavailable. Evidence: `evidence/runtime-command-path.md` (2026-09-09). Bash exposes `workdir`; explicit root and branch checks matched this worktree. The earlier runtime-tool blocker is resolved.
- [x] 1.3 Record hashes of historical plans and raw results for experiments 25–28, plus the preserved index locations. Verify later outputs use distinct destinations. Evidence: `evidence/historical-input-inventory.md` records SHA-256 hashes, read-only index locations and the distinct-destination rule; no later output exists yet.
- [x] 1.4 Before any push, identify the existing ten commits and inspect their complete committed diff for secrets and private data. Record the base, commit SHAs, commands and findings; stop on any unresolved finding. Evidence: `evidence/remote-backup.md` records `2c58b45..afe151e`, all ten SHAs, direct git-range gitleaks over 38 paths and 47 intermediate blobs, and zero leaks.
- [ ] 1.5 Before any push, back up ALL uncommitted changes locally, including staged, unstaged, untracked and intentionally edited ignored files. Keep the backup outside the pushable tree. Verify its inventory, SHA-256 hashes and readability against the source without changing the worktree. PARTIAL: `evidence/remote-backup.md` records a readable 20-file archive, SHA-256, and 20/20 byte comparison. Literal proof that every ignored file was included before push is absent; do not claim it. Mitigation (2026-09-10): `evidence/apply-resume-verification-2026-09-10.md` records a fresh verified backup of the current uncommitted planning files plus `.env`, `.pi/hindsight` and `.pytest_cache` (12/12 byte-identical, `.venv` excluded as reproducible from `uv.lock`); the checkbox stays open for the operator because pre-push proof is permanently unavailable.
- [x] 1.6 After 1.4 and 1.5 pass, push only the inspected ten-commit HEAD to the remote feature ref. Verify the remote SHA matches the approved local HEAD, all uncommitted work remains unchanged, and no pull request was created. Evidence: `evidence/remote-backup.md` records successful `2c58b45..afe151e` push and matching local/remote `afe151e93dd9b79e18618811dc15e35c6a96b16d`; no pull request was created.

## 2. Reopen the unapproved OCR decision

Settled (2026-09-10): the operator settled the OCR routing/default disposition. ADR-064 records the settled decision (OCR off by default, `mixed` removed from `OCR_UNCONDITIONAL_TYPES`, further promotion deferred). The tasks below record the historical reopen-and-correct work; their "pending" wording is preserved as provenance.

- [x] 2.1 Use the update-change workflow to reopen the OCR portion of original task 5.5 and the unapproved completion at 6.9. Verify earlier tokenizer approval and completed measurements remain recorded. Evidence: `improve-rag-input-quality-5/tasks.md` now carries dated recovery amendments; 5.5a/5.5b preserve the tokenizer promotion and instruction result, unchecked 5.5c and 6.9b reopen the OCR decision.
- [x] 2.2 Correct ADR-064 and ADR-062's newly attributed acceptance to show approval is pending. Verify the operator is not listed as having approved the outstanding decision. Evidence: ADR-064 title/status/deciders and decision 3 heading corrected with a dated recovery section; ADR-062 status returned to Proposed with a dated correction; neither ADR attributes the outstanding approval to the operator.
- [x] 2.3 Reconcile shared guide and environment-comment wording without adopting or discarding the preserved mixed patch. Verify the diff leaves packaged settings and production code unchanged. Evidence: `evidence/task-2.3-prose-reconciliation.md`; the wording distinguishes the committed unconditional `mixed` policy from the unapproved local threshold variant, the protected production/test hashes match apply-start, and the saved patch digest is unchanged.
- [x] 2.4 Synchronise the reopened decision with existing local and NiftyPM tracking through the normal workflow. Verify local state and cloud receipt separately; record unavailable cloud access as a blocker. Evidence: local state is reconciled and verified in `evidence/decision-tracking-sync.md`; `evidence/apply-resume-verification-2026-09-10.md` records the fresh worktree check (no `niftypm/` directory, no tracked NiftyPM file, no NiftyPM MCP server in this session). Cloud access is unavailable, so no cloud change or receipt is claimed. Revisit the cloud sync when NiftyPM access is configured.

## 3. Make experiment 25 rebuilding usable and safe

DESCOPED (2026-09-10): tasks 3.1–3.6 built an offline estimate, a spending-approval path and a budget-stop embedder for a paid rebuild. The operator confirmed at 3.7 that experiment 25 is not rerun; its historical PASS and tokenizer promotion stand. That path is not needed. Do not resume the `BudgetStopEmbedder` debug. The safe behaviour is the existing unconditional refusal in `build_index.py`. The lesson (a paid rebuild needs an offline estimate and explicit approval before spending) lives in the `experiment-validity-gates` spec, requirement "Paid rebuilds require current estimates and approval". The historical scaffold and its tests are absent from the consolidated tree. Keep their recovery records; do not restore that abandoned work.

- [ ] 3.1 Add offline tests for missing approval, stale estimates, forced rebuilds and protected destinations. Verify unsafe build attempts cannot make embedding requests. REOPENED (2026-09-10): the earlier 32 tests do not certify the recovered active builder interfaces. Current contracts cover an injected `EffectiveSettings`, full plan/code/lock identity, private copied-corpus recounting, request hashes, exclusive single-use destinations, `max_retries=0` before client creation, token caps and temporary markers. The end-to-end build tests remain blocked by `BudgetStopEmbedder`; see `evidence/exp25-debug-pause-2026-09-10.md`.
- [ ] 3.2 Add a production-preparation dry run with a network-blocked request recorder. Verify request-text parity against the installed adapter on synthetic fixtures; prove the parity test fails when normalisation is removed. PARTIAL (2026-09-10): the installed adapter is used through injected `EffectiveSettings`. Guarded request-accounting and request-hash contracts pass. The expanded estimator matrix needs a fresh guarded run after the recovered builder changes. End-to-end verification remains blocked by `BudgetStopEmbedder`; see `evidence/exp25-debug-pause-2026-09-10.md`.
- [ ] 3.3 Produce identified baseline/candidate request-token estimates and an explicit pricing-based cost estimate. Display token counts, percentage increase and approximate cost before requesting spending approval. Verify incomplete preparation and tokenizer fallback invalidate estimates; exceeding the 15% acceptance threshold does not. PENDING (2026-09-10): full settings, plan, code and lock identity checks exist. The merge requires explicit dated pricing and rejects non-finite prices. Recovered fragments remain unpriced; a verified monetary basis is still outstanding. No new full-corpus estimator pass was authorised. Code changed after historical identity checks, so prior estimates are stale. Do not treat this work as a spending approval.
- [ ] 3.4 Replace the unconditional refusal with estimate-bound approval and a separate build destination. Verify a valid, explicitly approved build can proceed above 15%, while the measured result still uses the frozen acceptance threshold. Verify `--force` cannot bypass approval and changed inputs invalidate it; retain the retrospective verifier. BLOCKED (2026-09-10): the recovered builder reaches `LanceVectorStore.write_nodes`, but `BudgetStopEmbedder` is not a LlamaIndex `BaseEmbedding`. The guarded success-build and repeated-`--force` contracts fail there. Proposed experiment-only repair: subclass `BaseEmbedding` while preserving guarded batching and accounting. It is not applied. See `evidence/exp25-debug-pause-2026-09-10.md`.
- [ ] 3.5 Update the overnight entry point and rebuild instructions. Verify no entry point uses the deprecated counter to approve spending and marker reuse checks run identity. PARTIAL (2026-09-10): `overnight.sh` still refuses rebuilds and makes no counter-based approval. Identity-bound marker reuse is covered by the recovered builder contracts, but cannot pass on the live path while `BudgetStopEmbedder` blocks vector-store creation. Rebuild instructions are stale after this diagnosis and need revision after an approved repair. `report.py:366` still prints the removed `--resume` flag (5.1 scope).
- [ ] 3.6 Add a dated TDR-023 follow-up documenting the repaired pre-spend path. Verify its claims link to passing tests and distinguish estimates from actual billing. PENDING (2026-09-10): the pre-spend path is not repaired. The proposed embedding-wrapper repair is paused. A follow-up can only claim a repaired path after an approved implementation and fresh guarded verification.
- [x] 3.7 Ask whether an actual rebuild is needed after offline verification. Record the operator's decision; if requested, obtain purpose, estimate and destination approval before adding execution tasks through update-change. Operator decision (2026-09-10): "Do not rerun Experiment 25." No execution tasks or spending approval were added.

## 4. Repair experiment 26 and 27 safeguards

- [x] 4.1 Add regression tests using experiment 26's four-query prefix and experiment 27's one-query prefix. Confirm the current code incorrectly passes them before implementing the fix. Evidence: `evidence/task-4-checkpoint-repair.md` records 24 fail-before failures; `evidence/apply-resume-verification-2026-09-10.md` records the fresh 38-pass focused run.
- [x] 4.2 Validate exact membership, uniqueness, categories and completed-row consistency before verdicts. Verify partial runs are INCOMPLETE and duplicate, mismatched or malformed runs are INVALID. Evidence: `evidence/task-4-checkpoint-repair.md` §4.2 and the fresh focused run in `evidence/apply-resume-verification-2026-09-10.md`.
- [x] 4.3 Validate compatible corpus, query, plan, index and treatment provenance, and pair rows by ID. Verify equal counts with mismatched identities cannot produce a verdict. Evidence: `evidence/task-4-checkpoint-repair.md` §4.3 and the fresh focused run in `evidence/apply-resume-verification-2026-09-10.md`.
- [x] 4.4 Isolate smoke outputs and bind new checkpoint resume to run identity. Verify legacy evidence is readable with disclosed provenance limits and cannot silently mix with a new run. Evidence: `evidence/task-4-checkpoint-repair.md` §4.4 and the fresh focused run in `evidence/apply-resume-verification-2026-09-10.md`.
- [x] 4.5 Record session identity, timestamps and interruptions for new measured runs. Verify resume reports mixed execution periods instead of asserting one uninterrupted network period. Evidence: `evidence/task-4-checkpoint-repair.md` §4.5 and the fresh focused run in `evidence/apply-resume-verification-2026-09-10.md`.
- [x] 4.6 Recompute the complete saved cells offline into a separate report location. Verify all 223 expected queries and the original FAIL verdicts; compare raw-file hashes before and after. Evidence: `evidence/task-4-checkpoint-repair.md` §4.6; a fresh 25/25 SHA-256 check is recorded in `evidence/apply-resume-verification-2026-09-10.md`.

## 5. Rewrite the explanations and decide about repeats

- [x] 5.1 Correct experiment 26's discussion and report generation. Verify equal R@5 is distinguished from changed rankings, and latency attribution is described as inconclusive. Evidence: `evidence/task-4-checkpoint-repair.md` §5.1 and `evidence/apply-resume-verification-2026-09-10.md` verify the dated 65-ranking-difference correction, inconclusive latency attribution and regenerated recovery report.
- [x] 5.2 Explain experiment 27's question and historical four-cell comparison in plain English. Verify the report states the PDF limitation and gives uncertainty for exploratory interaction claims. Evidence: `experiments/27-combined-candidate-path-2026-09-09/results.md` now opens with a plain-English `## Summary` (question, outcome, decision status), and a dated "Recovery clarification" section explains the four cells' provenance (exp 22 + exp 26 + two cells measured here), labels the +0.0118 interaction exploratory with cross-run limits, and keeps the no-PDF limitation. Same regeneration caveat as 5.1.
- [x] 5.3 Add dated rationale corrections without editing frozen plans, including the latency arithmetic and experiment 28's chosen 1% threshold. Verify no historical gate value changes. Evidence: dated "Recovery clarification (2026-09-09)" sections in experiments 26, 27 and 28 `results.md`: 2,850 ms is ~1.436× the 1,984.2 ms baseline (not the stated 1.5×); exp 28's 1% Wilson threshold is an operator-style practical choice, planned at n≈83 vs realised n=79. All 21 inventoried plan/raw JSON files re-hashed byte-identical after the edits; no gate value, verdict or raw measurement changed.
- [x] 5.4 Ask whether another experiment 26 or 27 run would answer a useful remaining question. Record rerun or defer for each; add an approved protocol and cost tasks only if requested. Decision recorded (2026-09-10): the operator authorised no paid repeat for either experiment. No protocol or cost tasks were added. The combined-candidate question is closed by the recorded decision; formal close-out of the deferred routing study is recorded in the design decision register (deferred to a separate future change).

## 6. Repeat the PDF routing study on an approved collection

DEFERRED (2026-09-10): this section is a fresh paid experiment (approved corpus, independent page labels, held-out split, real OCR run), not evidence repair. The concrete routing defect experiment 28 exposed is already fixed (commit `9bf4810`). What still needs a fresh preregistered study is a decision to enable OCR by default. That belongs in its own OpenSpec change. Tasks 6.1–6.11 move there; do not start them under this change.

- [ ] 6.1 Repair checkpoint identity and private mapping preservation for PDF classification. Verify added, removed or replaced documents and a changed policy refuse unsafe resume.
- [ ] 6.2 Remove unrestricted exception text from public outputs and check serialised artefacts for leaks. Verify an exception containing a synthetic private path cannot expose it in checkpoints or reports.
- [ ] 6.3 Ask the operator to approve the document list or precise selection rule, including the known book. Verify the approval is recorded privately before reading any selected PDF.
- [ ] 6.4 Create a separate repeat-study directory and immutable private digest-to-ID mapping. Verify discovery processes only the approved collection and leaves experiment 28's original output unchanged.
- [ ] 6.5 Agree an independent page-annotation method and development/held-out split. Record labels and uncertainty; verify the known book is development evidence and held-out outcomes do not guide candidate design.
- [ ] 6.6 Ask the operator to approve missing-page tolerance, whole-file routing behaviour and enable-only zero-threshold behaviour. Record the precise candidate and acceptance criteria; stop if undecided.
- [ ] 6.7 Freeze the new protocol, labels/split and criteria in a separate commit before held-out scoring. Verify the commit precedes candidate measurement; preserve all old gates.
- [ ] 6.8 Add tests for comparing the pinned historical policy with the approved candidate, using synthetic evidence first. Verify the uncommitted patch cannot silently become the baseline and removing a guard fails its regression test.
- [ ] 6.9 Run the approved classification/routing evaluation and record elapsed classification time. Verify complete membership, stable resume and public-output privacy; report uncertain labels explicitly.
- [ ] 6.10 Record whether real OCR measurement is authorised, including selected documents, timeout and runtime budget. If authorised, add and verify explicit execution tasks; otherwise label OCR cost as projected and recovery quality unmeasured.
- [ ] 6.11 Publish the routing comparison with missing-page evidence, hold-out results and limitations. Verify fixture-based time estimates are labelled, convenience selection is disclosed, and negative results are retained.

## 7. Return the routing and promotion choices to the operator

- [x] 7.1 Present the measured routing results and ask whether to adopt a policy, defer it or request more evidence. Verify the decision register cites the operator's response and does not infer consent from gate results. Evidence: results presented 2026-09-10 with the explicit limit that experiment 28's measurements predate the fix; the operator replied "confirm deferral". The decision register cites that response; no consent is inferred from gate results. See `evidence/apply-resume-verification-2026-09-10.md`.
- [x] 7.2 If adoption is requested, use update-change to specify the exact PDF behaviour and implementation tasks before coding. Verify the amendment covers compatibility, diagnostics, timeout tests, routing identity invalidation and coherent documentation; if deferred, record that disposition. Update (2026-09-10): the routing bug fix (demote `mixed` from the unconditional set) is adopted as a standalone `fix:` commit `9bf4810`, with regression tests, because it corrects a defect on design grounds and does not change the packaged OCR default. Consolidation correction: source identity now includes the unconditional routing set and uses schema 5, including when OCR is off. A later ingestion attempt can reprocess an existing source; preserved experiment indexes remain untouched. The broader OCR default-promotion decision (enabling `OCR_FALLBACK_ENABLED`) stays deferred to the Section 6 future study. Evidence: the operator confirmed the deferral on 2026-09-10 (task 7.1); the disposition is recorded here and in the design decision register. Any further routing change requires an explicit amendment; leaving OCR off does not waive identity requirements.
- [x] 7.3 Ask separately about OCR default promotion and the original task 5.5. Verify the task remains open until the operator decides; preserve the historical tokenizer and instruction evidence. Evidence: the operator decided on 2026-09-10 that `OCR_FALLBACK_ENABLED=false` stays; original tasks 5.5c/6.9b closed with that recorded decision; the tokenizer promotion (5.5a) and instruction rejection evidence is preserved unchanged.

## 8. Verify and hand back the work

- [x] 8.1 Run targeted offline tests for all repaired tooling and confirm meaningful fail-before/pass-after evidence. Verify required tests ran rather than silently skipped for missing optional extras. Evidence: `evidence/apply-resume-verification-2026-09-10.md` — exp 26/27 38 passed with no skips; wider focused run 57 passed with the only 2 skips being the adapter-parity cases whose optional extra is absent (reason printed; descoped estimator scope). Fail-before records: 24 in `evidence/task-4-checkpoint-repair.md` plus the tripwire count-drift failure before its re-baseline.
- [x] 8.2 Run `uv run pytest -m "not slow" -q` through the verified cwd-capable tool. Record actual counts and failures; do not reuse the historical 2,779-pass claim. Evidence: green run at `23389f7` after the tripwire re-baseline — 2,821 passed, 131 skipped, 19 deselected, 0 failed in 551.82 s; recorded in `evidence/apply-resume-verification-2026-09-10.md` and `repair/evidence/fresh-fast-coverage.md`.
- [x] 8.2a Capture fresh full fast-suite branch coverage, using the 8.2 run if it uses the complete repository coverage command. Record the exact command, revision, lock provenance, test counts, skips and applicable coverage totals. If branch attribution remains unclear, run the identical command and configuration at a named comparable earlier revision in a separate clean location. Stop at measurement; do not add unrelated coverage fixes. Evidence: `repair/evidence/fresh-fast-coverage.md` now records `23389f7` (overall 91%, Core+MCP 91%, Orchestration 94%) alongside the identical-command `afe151e` measurement, so attribution needs no further earlier-revision run. The Core+MCP 95% shortfall stays visible and unresolved; no unrelated coverage fix was attempted.
- [x] 8.3 Run the required security review on changed code and check Python file lengths. Verify any unresolved finding stays visible; do not mark blocked scans complete. Evidence: fresh opengrep scan of all changed paths — 21 `AIK_py_LFI` findings traced to guarded run-name sinks or pytest-tmp fixtures, dispositions and SARIF in `evidence/aikido-scan-2026-09-10.sarif` and `evidence/apply-resume-verification-2026-09-10.md`; residual LOW local-symlink risk stays visible. No `src/omrg` file exceeds 500 lines; the two named violators were removed with the descoped scaffold. Experiment 28 privacy/checkpoint-identity items remain open in the deferred future study.
- [x] 8.3a Reassess both prior `AIK_py_LFI` findings for checkpoint and temporary-file paths with a fresh scan. For each finding, trace reachable input paths from public or experiment boundaries to the flagged operation and record trust boundaries, controls, evidence and disposition. Do not reuse an earlier scan conclusion; keep every reachable or unresolved finding visible. Evidence: `evidence/aikido-reassessment.md` traces fixed module-defined paths and records no arbitrary CLI path, plus the residual LOW local symlink risk. The prior exact scanner payload is absent.
- [x] 8.4 Validate this change and the updated original change with OpenSpec strict validation. Verify original evidence hashes, preserved indexes and packaged defaults match their expected state. Evidence: both changes pass `--strict`; 25/25 recompute-manifest hashes re-verified (includes the 21 inventoried files); experiment 22 output and the experiment 25 `lancedb` index present read-only; `OCR_FALLBACK_ENABLED` stays off in `.env.example`. See `evidence/apply-resume-verification-2026-09-10.md`.
- [x] 8.5 Write a short completion note with completed tasks, deferred runs and outstanding decisions. Verify no unchecked work or unresolved coverage exception is described as complete. Evidence: `evidence/completion-note-2026-09-10.md` lists the session completions, descoped/deferred work, the unresolved Core+MCP coverage exception and the outstanding operator decisions (1.5, 7.1, 7.3).

## Review amendment after consolidation at `047dedb`

Source inspection found an OCR import-boundary regression and remaining report
safety gaps. The review fixes keep schema 5, share the policy through a pure
integration module, reject missing or malformed selected checkpoints, check
session inputs, use unrounded gate values, and require fresh report directories.
Historical plans, measurements, reports and repair evidence remain unchanged.

- [x] 8.6 Validate the review fixes locally: OCR/identity tests, experiment summary safety tests, import contracts, lint/format, full fast-suite branch coverage and OpenSpec strict validation. Verify the exact test-count adjustment and report all skips. Evidence: `evidence/review-fix-validation-2026-09-11.md` — at HEAD `b222823` (review-amendment revision): targeted OCR/identity, summary-safety, import-contract and verdict-validity subsets 174 passed, 0 skipped, 0 failed in 23.83 s; `ruff check` and `ruff format --check` clean; `openspec validate --all --strict` 53/53; full fast suite with branch coverage 2,863 passed, 131 skipped, 19 deselected, 0 failed in 597.65 s (2,863 = 2,858 tripwire subprocess count + 5 tripwire-own cases; the tripwire `test_base_skip_manifest_is_exact` passed inside the run, validating the 2,858/131/19 pin at this revision). Coverage: overall 91%, Orchestration 93%, Core+MCP 90% (below its 95% floor — a 1-point regression from the 91% at `23389f7` caused by the review amendment's new core code; the exception remains unresolved and visible, no unrelated coverage fix attempted). The optional-adapter description is corrected and verified against the live environment: the OpenAI-like adapter is absent, the Qwen3-Embedding-4B tokenizer cache and the historical ground truth are present, so 2,858/131 holds; "all three present" would yield 2,867/122, not 2,858/131.
- [x] 8.7 Confirm the existing independent backup of the actual Experiment 22/25 databases, shared corpus and ignored ground truth/manifests. Supply source and backup locations plus checksum/count verification privately. GitHub verifies tracked evidence only; it cannot establish that the external data was backed up. Do not rebuild missing data. Evidence: `evidence/private-backup-verification-2026-09-11.md` records the existing `backup-20260910-consolidation` backup (Experiment 22/25 LanceDB databases, the 10,025-file shared corpus and the ignored experiment 22 ground truth) outside the pushable tree. Sampled SHA-256 checksums matched the live source for the ground truth (full hash), three data fragments each from the exp22 and exp25 Lance indexes, and one corpus fragment; corpus file counts matched exactly (10,025 = 10,025). No data was copied, rebuilt or regenerated for this record.
