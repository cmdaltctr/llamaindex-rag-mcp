# Tasks: improve-rag-input-quality-5

Implement in stages. Do not promote OCR thresholds, chunk-size defaults, or query-instruction defaults until the corresponding experiment passes.

## 1. Pin baselines and fixtures

- [x] 1.1 Add representative PDF fixtures for clean single-column text, clean two-column/table content, scanned content, image-based content, and mixed content. Keep fixtures small and licence-safe. Split them into a committed **calibration** set and a disjoint committed **evaluation** set, and record which files belong to which.
- [x] 1.2 Add baseline assertions for `pdf-inspector` output: `pdf_type`, confidence, `pages_needing_ocr`, page count, emitted Markdown, and current degraded behaviour when OCR may be required.
- [x] 1.3 Add a Markdown fixture containing headings, paragraphs, a table, and a list. Record current MarkdownNodeParser/SentenceSplitter chunk boundaries and token-size behaviour.
- [x] 1.4 Add a regression fixture that demonstrates why the current four-characters-per-token small-chunk estimate can disagree with the Qwen tokenizer.
- [x] 1.5 Add a Qwen retrieval baseline using the current raw query path. Record Evidence Recall@1/@3/@5, MRR/nDCG where available, latency, and representative identifier-heavy queries.
- [x] 1.6 Freeze the promotion gates **before any candidate is evaluated**, not merely before a default changes. For each of the three experiments, write into its machine-readable plan (per `experiment-validity-gates`) a numeric quality limit, a numeric regression limit on the workload the candidate is most likely to damage, and a numeric latency/cost limit at a stated percentile. Commit them in their own commit so the record shows they preceded the measurements. Do not invent the values here; produce them from the Stage 1 baselines.
- [x] 1.7 Calibrate the OCR routing gate on the calibration fixtures only. Sweep the confidence threshold and the `pages_needing_ocr` proportion, and report for each candidate gate how many text-based PDFs it would route to OCR and how many OCR-required PDFs it would leave on the fast path. Report routing behaviour only — retrieval quality belongs to the held-out evaluation set in stage 5.
- [x] 1.8 Define the routing gate's configuration shape before implementation: top-level fields on `EffectiveSettings` beside `pdf_reader` and `liteparse_ocr_enabled`, resolved once at the composition root and injected. Confirm the chosen names do not collide with the retired-variable tripwire in `config/legacy.py`.
- [x] 1.9 Record the baseline `source_index_identity` payload shape and the current `_INDEX_IDENTITY_SCHEMA` value, so the stage 2/3 identity extension is a visible, reviewable diff rather than an incidental change.

## 2. Add PDF routing and an isolated PaddleOCR-VL worker

- [x] 2.1 Add a separate OCR worker project with its own dependency manifest and lockfile. Declare Python `>=3.11,<3.14` and keep PaddleOCR, PaddleX, PaddlePaddle, model-runtime, and accelerator packages in that worker lock only. Verify OMRG's main package metadata, root lockfile, and main install contain no Paddle packages.
- [x] 2.1a Add a deterministic worker provisioning command that uses the worker-owned lockfile. Verify supported Python 3.11, 3.12, and 3.13 environments resolve without changing OMRG's main environment, and reject unsupported interpreters with an actionable error.
- [x] 2.1b Add an import-boundary test proving modules under `src/omrg/` contain no Paddle imports and OMRG starts without the worker environment.
- [x] 2.2 Add the isolated PaddleOCR-VL worker entry point and its OMRG-side adapter. Use the full document pipeline and emit structured Markdown plus honest reader/OCR metadata.
- [x] 2.2a Define and implement the UTF-8 versioned JSON Lines protocol over worker standard input and output. Include request identifiers, protocol versions, terminal success/error envelopes, and an explicit output-schema identity and version. Verify each request and response occupies one complete line and remains correlated.
- [x] 2.2b Reserve worker standard output for protocol messages. Drain standard error independently, then forward diagnostics through OMRG logging without merging the streams. Add a test that fills or emits on standard error while standard output remains valid JSON Lines and the worker does not block.
- [x] 2.3 Add the smallest routing seam around the existing `pdf-inspector` result. Clean extractable text-based PDFs MUST stay on `pdf-inspector`; scanned/image-based/mixed or otherwise qualified OCR-required PDFs use the isolated worker when available.
- [x] 2.4 Route the whole PDF through the worker in the OCR branch. Do NOT add page-level `pdf-inspector`/worker stitching in this change.
- [x] 2.5 Keep multi-column or table-heavy **text-based** PDFs on `pdf-inspector` when its inspection/extraction quality is acceptable. Layout complexity alone MUST NOT select OCR.
- [x] 2.6 Add a metadata-only capability probe at the composition boundary and inject its resolved result. Report availability; protocol version; package names and exact versions; pipeline identity and revision; model identity and revision; and output-schema identity and version. Verify the probe does not initialise Paddle, import model code, load weights, or leave the long-lived parsing worker running. Return one stable unavailable fingerprint for missing, unusable, malformed, or incompatible workers. Do not probe from `config/` or introduce a settings singleton.
- [x] 2.6a Add owner-scoped lifecycle control for the parsing worker. Permit one request in flight, start one process on the first dispatched OCR request, reuse it while healthy, close it during engine shutdown, and discard it after timeout, crash, unexpected output closure, or protocol failure. Verify a later OCR request starts a fresh process without replaying the failed request.
- [x] 2.6b Add injected settings for the worker command, environment location, and request timeout. Verify no machine-specific path is hardcoded, and keep these operational settings separate from the calibrated routing gate.
- [x] 2.7 When OCR is required but the worker is absent, unprovisioned, incompatible, or unusable before dispatch, preserve `pdf-inspector`'s partial Markdown, mark the result as degraded, and emit an actionable diagnostic rather than fabricating content or failing the whole batch.
- [x] 2.8 Add tests proving a clean PDF never starts the long-lived worker or loads its model, the first OCR dispatch starts it, later OCR requests reuse it, and the unavailable path degrades deterministically.
- [x] 2.9 Preserve the existing per-file reader error boundary. After a complete parse request is written and flushed, a timeout, crash, unexpected output closure, malformed or mismatched protocol response, or structured worker error MUST return a structured error for that file. It MUST NOT mark the failed source version current or stop subsequent files, and the failure-safe replacement path MUST preserve any prior current version.
- [x] 2.9a Add post-dispatch timeout, crash, output-closure, protocol-failure, and worker-error tests. Verify each produces the correct structured per-file error, does not substitute partial Markdown as success, preserves any prior current version, continues the batch, invalidates the worker, and does not replay the failed request automatically.
- [x] 2.10 Emit the additive OCR diagnostics `ocr_required`, `ocr_used`, `ocr_backend`, and `pages_needing_ocr` on both PDF branches. Store `pages_needing_ocr` as a store-compatible scalar count, not the list `pdf-inspector` returns — nothing sanitises metadata values before the vector store.
- [x] 2.11 Add those four keys to `EXCLUDED_EMBED_METADATA_KEYS` in `core/ingestion/source_state.py` — the single central set `stamp_source_lineage` applies on every ingest path. Do not add a second exclusion list in the PDF reader.
- [x] 2.12 Add tests proving the four keys are absent from `MetadataMode.EMBED` and `MetadataMode.LLM` text, present in stored metadata and in retrieval result rows, and that an operator can tell fast path from OCR fallback from degraded extraction by reading them.
- [x] 2.13 Extend `build_index_identity` with the OCR routing configuration and the canonical resolved worker fingerprint: availability; protocol version; package names and exact versions; pipeline identity and revision; model identity and revision; and output-schema identity and version. Use a stable unavailable fingerprint and explicit keyword arguments alongside `text_format` and `embed_model`. Exclude transient process details, bump `_INDEX_IDENTITY_SCHEMA` once for the shared Stage 2/3 payload change, and do not add a second identity mechanism.
- [x] 2.14 Add identity-driven re-ingestion tests. Prove a byte-identical source indexed while the worker was unavailable is re-ingested once it becomes available. Prove changes to each protocol, package, pipeline, model, and output-schema fingerprint field also prevent `skipped_unchanged`, while an unchanged canonical fingerprint preserves existing unchanged-source behaviour.
- [x] 2.15 Add a dedicated worker smoke-test command or job, separate from the main OMRG test suite. Provision from the worker lock under Python 3.11, 3.12, and 3.13; run the capability probe; parse one small licence-safe OCR fixture; validate structured Markdown, protocol framing, and output schema; and confirm no Paddle package enters the main environment.

## 3. Add structure-aware model-token Markdown chunking

- [x] 3.1 Add `semantic-text-splitter` and Hugging Face `tokenizers` as explicit dependencies and record tested dependency floors.
- [x] 3.2 Add `embedding.tokenizer_model` and `embedding.tokenizer_revision` to the pure settings model and frozen `EmbeddingBlock`; expose them as `EMBEDDING__TOKENIZER_MODEL` and `EMBEDDING__TOKENIZER_REVISION`. Keep them empty/unset until explicitly configured or promoted by evidence.
- [x] 3.3 Add a cached Hugging Face tokenizer loader keyed by tokenizer identity **and revision**. For the Qwen experiment use `Qwen/Qwen3-Embedding-4B`. Call `no_truncation()` on the resolved tokenizer before it is used as a size calculator: a tokenizer loaded with truncation enabled caps every long count and would silently under-measure chunks.
- [x] 3.3a Make the loader work from the local Hugging Face cache with no network access, and add a test that resolves offline. Ingestion must not acquire a hidden runtime dependency on a remote hub.
- [x] 3.4 Add the Rust-backed `MarkdownSplitter` path for structured Markdown when a model-matched tokenizer is configured. Prefer complete headings, paragraphs, tables, lists, and sentences that fit before splitting at smaller boundaries.
- [x] 3.5 Derive `header_path` on the new path from the source Markdown itself: use the splitter's chunk-offset API to locate each chunk and resolve the enclosing heading chain, then write the path before the recovery hooks run. `ensure_heading_metadata` only copies metadata a parent node already carries, and the Rust splitter emits text with no parent node, so it cannot be the source of heading ancestry here. Keep it in the pipeline as the idempotent guard it already is, and keep optional heading prepend and the other recovery hooks around the new splitter.
- [x] 3.5a Pass the configured `CHUNKING__CHUNK_OVERLAP` to the splitter as its own overlap in tokenizer units. Validate `chunk_overlap < markdown_chunk_size` at the composition boundary and fail naming both settings, because the splitter rejects an overlap at or above the capacity.
- [x] 3.5b Enforce the token cap over the chunk text **as finalised for embedding**, prepended heading included. When heading prepend is enabled, reduce the splitter capacity by the token length of the prefix that chunk will receive, so a prepended chunk cannot exceed the budget. Do not define the cap over the complete LlamaIndex embedding payload: retained metadata is added after chunking and is not known when the splitter runs.
- [x] 3.6 Replace the Markdown small-chunk four-character token estimate with the real configured tokenizer count on the model-token-aware path.
- [x] 3.7 Keep `CHUNKING__MARKDOWN_CHUNK_SIZE` as the initial token budget. Do not change its packaged default in this stage.
- [x] 3.8 Keep plain-text, code, config, AST, and other non-Markdown strategies unchanged.
- [x] 3.9 Add tests proving a table or other complete Markdown unit stays together when it fits, oversized structures split without exceeding the configured Qwen-token budget, and non-Markdown output is unchanged.
- [x] 3.9a Add a heading-derivation test: nested headings in, correct `header_path` on every emitted chunk out, derived without a parent node.
- [x] 3.9b Add a heading-prepend-at-the-limit test: a section sized to fill the cap exactly, prepend enabled, and the finalised chunk still within `CHUNKING__MARKDOWN_CHUNK_SIZE` tokenizer units.
- [x] 3.9c Add an overlap test in tokenizer units, and a rejection test for `chunk_overlap >= markdown_chunk_size` failing at the composition boundary.
- [x] 3.9d Add a truncation test: a text longer than the tokenizer's truncation limit reports its full token count, proving `no_truncation()` took effect.
- [x] 3.9e Make the model-token structural regressions run on an in-memory tokenizer built in the test, so none of them depends on a populated model cache. Keep one end-to-end check on the real cached tokenizer that skips when it is absent.
- [x] 3.9f Add regressions for the heading-derivation and budget edges: a `#` line inside a backtick or tilde code fence must not join the heading chain; a skipped heading level must produce sibling paths, not nested ones; a heading prefix that leaves a capacity at or below the requested overlap must fail naming the settings; and a chunk that cannot be reduced must fail rather than be emitted above the cap.
- [x] 3.10 When the configured tokenizer cannot be loaded, do not claim model-token-aware chunking. Preserve the current Markdown splitter path with a clear diagnostic.
- [x] 3.11 Extend `build_index_identity` with the tokenizer identity and revision and the **resolved** active splitter (model-token-aware or legacy fallback), using the same explicit keyword-argument path as task 2.13 and the same schema bump. The resolved value is required: recording only the configuration would leave a corpus chunked under the fallback matching forever once the tokenizer became loadable.
- [x] 3.12 Add a test proving degraded-chunking recovery: a byte-identical Markdown source indexed while the tokenizer could not be resolved MUST NOT be reported `skipped_unchanged` once it resolves.

## 4. Add query-only embedding instructions

- [x] 4.1 Add `embedding.query_instruction` to the pure settings model and frozen `EmbeddingBlock`; expose it as `EMBEDDING__QUERY_INSTRUCTION`. Empty means current raw-query behaviour.
- [x] 4.2 Add a generic embedding-query preparation seam that produces `Instruct: <instruction>\nQuery: <query>` only when an instruction is configured.
- [x] 4.3 Apply query preparation immediately before query embedding. Indexed/document embedding text MUST remain unchanged.
- [x] 4.4 Keep dense retrieval model-agnostic. Do NOT add Qwen/provider/model-substring `if/elif` dispatch in `dense.py`. Preserve the injected embedder/engine-owned cache path; the legacy `Settings.embed_model` fallback in `dense.py` is out of scope for this change and must be left as it is, not removed or extended.
- [x] 4.4a Confine preparation to the dense branch. `search()` passes the same raw query to the sparse runner and to the reranker, so add a test proving the sparse backend and the cross-encoder receive the raw query, never the instructed one.
- [x] 4.5 Change query-embedding cache identity to `(prepared_query, embedding_model_name)` so equal raw queries with different instructions cannot collide.
- [x] 4.6 Add tests for empty-instruction backward compatibility, instructed query formatting, unchanged document embeddings, cache reuse for identical prepared queries, and cache separation when the instruction changes, isolation between engines with the same model name, and cache release on engine close.
- [x] 4.7 Use the first experiment candidate: `Given a user query, retrieve passages that provide relevant and accurate evidence for answering the query.` Do not make it a packaged default yet. _(Evaluated verbatim as experiment 26's `candidate_instruction` cell and again as experiment 27's `combined_candidate` cell. Never made a packaged default — experiment 26 FAILED its frozen gates.)_
- [x] 4.8 Keep `embedding.query_instruction` **out** of `build_index_identity`. Add a test proving that changing it alone leaves the stored index identity unchanged and a byte-identical source still reports `skipped_unchanged`.

## 5. Run isolated and combined evaluations

- [x] 5.0 Before running any candidate cell, confirm the frozen gates from tasks 1.6 and 1.7 are committed and unchanged. If a gate is missing, stop and produce it — a gate written after the candidate numbers exist is not a gate.
- [x] 5.1 PDF ablation: compare current `pdf-inspector` output with the routed isolated-worker candidate on the **held-out evaluation fixtures**, using the gate calibrated in task 1.7. Record reading order, table/structure fidelity, missing content, failure rate, latency, and downstream evidence retrieval. _(Experiment 24, verdict PASS on all five frozen gates. Failure rate and latency: `output/eval_results.summary.json`. Reading order, structure/table fidelity, missing content and downstream evidence retrieval: `output/task_5_1_analysis.json` and the "Task 5.1 wording" section of `results.md`. Table fidelity is recorded as unmeasurable — the held-out pages carry no tabular content.)_
- [x] 5.2 Chunking ablation: build comparable indexes from the same canonical Markdown using the current splitter and `semantic-text-splitter` + Qwen tokenizer. Record Evidence Recall@1/@3/@5, Evidence MRR, section/hierarchy Match@1, nDCG, chunk-size distribution, and ingestion time. Record in the runtime manifest the resolved tokenizer identity **and revision**, the proportion of chunks carrying a derived `header_path` on each path, and the worst-case gap between chunk-text tokens and full embedding-payload tokens. _(Experiment 25, verdict PASS; chunker promoted at 5.5 — ADR-063 Accepted.)_
- [x] 5.3 Query-instruction ablation: hold the index fixed and compare raw Qwen queries with the candidate instruction, including semantic and identifier-heavy technical queries. _(Experiment 26, verdict FAIL. Paired R@5 lift −0.0246 against a frozen +0.0300; identifier-heavy R@10 0.2563 against 0.2583. Latency gate failed for both arms on a slow provider day and did not discriminate — the raw arm was the slower of the two. Negative result committed; instruction NOT retuned.)_
- [x] 5.4 Combined run: compare the full candidate path against the current production-shaped baseline and record both quality and cost/latency. _(Experiment 27, a 2×2 over chunking × instruction, verdict FAIL on quality and latency, regression gate PASS. Combined R@5 0.2237 against the frozen 0.231; the failure is entirely the instruction — the chunker's own arm cleared the bar again at 0.2365. Cost: index reused unchanged (exp 25's verified 0.974906 build ratio), 446 query embeddings, instruction adds 22.9 tokens to a 450-token mean query. **Scope limit recorded:** FreshStack has no PDFs, so OCR routing is not exercised; the full path is measured in two disjoint halves, retrieval here and PDF extraction in experiment 24.)_
- [x] 5.5 Promote only values that pass the frozen gates. If a threshold, chunk size, or instruction does not qualify, leave it opt-in, keep the packaged default unchanged, and commit the negative result alongside the positive ones rather than tuning around the gate or deleting the run. _(Settled 2026-09-10: OCR stays off by default; `mixed` removed from `OCR_UNCONDITIONAL_TYPES`; further OCR promotion deferred.)_
  - [x] 5.5a Model-token Markdown chunking remains promoted: experiment 25 passed its frozen gates and ADR-063 records the packaged tokenizer identity and revision.
  - [x] 5.5b The query-instruction result remains recorded: experiment 26 failed its frozen gates, the instruction remains empty by default, and it was not retuned.
  - [x] 5.5c OCR routing/default disposition settled by the operator on 2026-09-10: `OCR_FALLBACK_ENABLED=false` stays; both routing thresholds remain `0.0`; `mixed` is removed from `OCR_UNCONDITIONAL_TYPES` and routes by the threshold gate. Experiments 24 and 28 remain historical evidence, including the failed experiment 28 gates. Any promotion requires the future preregistered routing study. See ADR-064's settled decision.

## 6. Documentation, validation, and decision record

- [x] 6.1 Update the ingestion/PDF guide with the `pdf-inspector` fast path, OCR routing rules, isolated worker provisioning and lifecycle, protocol/version diagnostics, unavailable degradation, post-dispatch failure behaviour, and emitted OCR metadata. _(New "OCR fallback for scanned PDFs" section in `docs/guides/ingestion.md`; settings tables in `docs/guides/configuration.md`; `OCR*WORKER*_`examples added to`.env.example`.)\*
- [x] 6.2 Update the chunking/configuration guides with `EMBEDDING__TOKENIZER_MODEL` and `EMBEDDING__TOKENIZER_REVISION`, model-token-aware Markdown behaviour, the token-cap and heading-prepend contract, and the fact that the embedding inference provider is independent from the tokenizer used for token budgeting. \_(ingestion.md chunk-size guide split into character/token budgets; configuration.md `EMBEDDING\_\__` table extended with the promoted defaults.)\*
- [x] 6.3 Document `EMBEDDING__QUERY_INSTRUCTION`, including that it applies to queries only and may change query-vector semantics without requiring document re-ingestion. _(`EMBEDDING__QUERY_INSTRUCTION` row plus the `#### EMBEDDING__QUERY_INSTRUCTION` subsection in `docs/guides/configuration.md`.)_
- [x] 6.4 Update `.env.example` with commented examples for the `Qwen/Qwen3-Embedding-4B` tokenizer identity and revision, the OCR routing gate fields, and the evaluated query instruction. Do not add pre-v2 flat aliases. _(Tokenizer lines are active because task 5.5 promoted them to the packaged default; OCR gate values are the exp 23 calibrated 0.5/0.5; instruction stays commented pending exp 26.)_
- [x] 6.4a Document the re-ingestion consequence: the tokenizer identity, resolved splitter, OCR routing configuration, and resolved worker fingerprint all participate in the index identity. State that provisioning the worker or changing its protocol, packages, pipeline, model, or output schema invalidates previously indexed sources, including non-PDF ones. State that the query instruction deliberately does not. _(New "Index identity and re-ingestion" section in `docs/guides/configuration.md`.)_
- [x] 6.5 Run `uv sync` and targeted PDF/chunking/retrieval tests. _(`uv sync` clean — it removes the `openrouter` extra, which only the experiment runners need. Targeted OCR/PDF: 42 passed (`test_ocr_routing_seam`, `test_ocr_routing_gate`, `test_ocr_identity_reingestion`, `test_pdf_baseline_fixtures`). Targeted chunking/retrieval (`-k "chunk or markdown or tokenizer or retriev or dense or sparse"`): 421 passed, 35 skipped.)_
- [x] 6.6 Run `uv run pytest -m "not slow" --cov=omrg --cov-branch` and confirm the existing coverage floors hold. _(**2,779 passed, 131 skipped, 0 failed** in 585 s. Overall branch coverage **90.7%** — the ≥90% overall floor holds. Orchestration tier **93.8%** branch — the ≥85% floor holds. **The core+MCP ≥95% tier floor does NOT hold: 90.6% branch, 92.2% line.** The shortfall is concentrated in Chroma vector-store modules (`chroma.py` 23%, `naming.py` 22%, `paged.py` 50%, `identity.py` 55%), because 91 of 162 Chroma tests skip in the fast suite. `git diff origin/v3...HEAD -- src/omrg/core/vectordb/` is empty: this change touches none of them, so the shortfall is pre-existing, not introduced. This change's own modules are well covered — `ocr_routing.py` 97%, `factory.py` 100%, `tokenizer.py` 100%, `protocol.py` 89%. Per AGENTS.md and TDR-007 the tier project checks are currently informational while the targets are recalibrated for branch measurement; the blocking patch check is unaffected.)_
- [x] 6.7 Run the dependency-floor job/test for new main dependencies, then verify the isolated worker lock separately. Confirm the main dependency graph remains free of Paddle packages. _(`tests/test_dependency_floors.py` passed. `tests/test_ocr_worker_import_boundary.py` 4 passed. Main graph verified Paddle-free three ways: no `paddle` string in `pyproject.toml` or the root `uv.lock`, and `importlib.util.find_spec` returns None for `paddle`, `paddleocr` and `paddlex` in the synced main environment. The worker lock is separate and does hold them — `ocr-worker/uv.lock` pins `paddleocr[doc-parser]` and `paddlepaddle` across 283 packages.)_
- [x] 6.8 Run `openspec validate improve-rag-input-quality-5 --strict` and fix any stale baseline requirement rather than working around the validator. _(Clean: "Change 'improve-rag-input-quality-5' is valid". No baseline spec needed correcting.)_
- [x] 6.9 After the empirical decisions are confirmed, write an ADR recording the isolated worker boundary, lifecycle and protocol, accepted OCR routing gate, tokenizer/chunking choice, query-instruction policy, failure behaviour, the frozen gates and whether each candidate met them, and any defaults that were actually promoted. Record rejected candidates and their negative results in the same ADR. _(Settled 2026-09-10: ADR-064 records the settled OCR default decision; ADR-062 preserves the worker architecture record.)_
  - [x] 6.9a ADR-063 continues to record the accepted tokenizer/chunking choice; ADR-064 preserves experiment 24–28 evidence and frozen-gate outcomes.
  - [x] 6.9b Operator decision settled 2026-09-10: OCR stays off by default; `mixed` removed from `OCR_UNCONDITIONAL_TYPES`; ADR-064 records the settled decision; ADR-062 stays Proposed for the worker architecture.
