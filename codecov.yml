# Codecov configuration — replaces SonarCloud coverage gating.
# Coverage tiers mirror AGENTS.md:
#   core + MCP >= 95%, orchestration >= 85%, overall >= 90%.
# Project checks are informational (not CI-blocking) while targets are
# recalibrated for branch coverage — see TDR-007 amendment.

coverage:
  status:
    project:
      core:
        target: 95%
        informational: true
        paths:
          - "src/omrg/core/ingestion/"
          - "src/omrg/core/retrieval/"
          - "src/omrg/core/metadata/"
          - "src/omrg/core/chunking/"
          - "src/omrg/core/vectordb/"
          - "src/omrg/core/profiles/"
          - "src/omrg/core/settings.py"
          - "src/omrg/config/"
          - "src/omrg/transports/mcp.py"
      orchestration:
        target: 85%
        informational: true
        paths:
          - "src/omrg/daemon/"
          - "src/omrg/transports/cli/"
      default:
        target: 90%
        informational: true

    patch:
      default:
        target: 90%

ignore:
  - "tests/**"
  - "experiments/**"
  - "**/__init__.py"
  - "**/__pycache__/**"
  - ".venv/**"
  - "chroma_db/**"

comment:
  layout: "reach, diff, flags, files"
  behavior: default
  require_changes: false
